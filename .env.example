# Optional: point to the FastAPI backend documented in docs/AI_ASSISTANT_RUNBOOK.md
# When unset (or unreachable), Obat falls back to a built-in bilingual mock service.
VITE_API_BASE_URL=http://127.0.0.1:8787
